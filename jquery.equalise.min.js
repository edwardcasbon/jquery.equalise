(function($){$.fn.equalise=function(options){var settings={itemClass:"equalise-item"};$.extend(settings,options);return this.each(function(){var $this=$(this),height=0,items=$this.find("."+settings.itemClass);for(var i=0;i<items.length;i++){var $item=$(items[i]),itemHeight=$item.outerHeight();height=itemHeight>height?itemHeight:height}for(var i=0;i<items.length;i++){var $item=$(items[i]);$item.height(height-$item.css("paddingTop").replace(/[^-\d\.]/g,"")-$item.css("paddingBottom").replace(/[^-\d\.]/g,""))}})}})(jQuery);